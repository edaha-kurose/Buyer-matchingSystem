# ============================================
# AIプレゼン自動スクリーニングシステム 環境変数設定
# ============================================
# このファイルを .env にコピーして、実際の値を設定してください
# cp .env.example .env

# --------------------------------------------
# アプリケーション設定
# --------------------------------------------
APP_NAME=AIプレゼン自動スクリーニングシステム
ENVIRONMENT=development  # development | staging | production
DEBUG=true

# --------------------------------------------
# データベース設定 (PostgreSQL)
# --------------------------------------------
# 開発環境 (docker-compose.dev.yml使用時)
DATABASE_URL=postgresql+asyncpg://screening:screening123@localhost:5434/ai_screening

# 本番環境例
# DATABASE_URL=postgresql+asyncpg://user:password@db-host:5432/ai_screening

# --------------------------------------------
# Redis設定
# --------------------------------------------
REDIS_URL=redis://localhost:6381

# 本番環境例（認証あり）
# REDIS_URL=redis://:password@redis-host:6379/0

# --------------------------------------------
# JWT認証設定 (重要: 本番環境では必ず変更!)
# --------------------------------------------
# 以下のコマンドで安全なキーを生成:
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-super-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# --------------------------------------------
# OpenAI API設定
# --------------------------------------------
OPENAI_API_KEY=sk-your-openai-api-key-here

# --------------------------------------------
# MinIO (S3互換ストレージ) 設定
# --------------------------------------------
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_BUCKET=proposals
MINIO_USE_SSL=false

# 本番環境 (AWS S3使用時)
# MINIO_ENDPOINT=s3.amazonaws.com
# MINIO_ACCESS_KEY=your-aws-access-key
# MINIO_SECRET_KEY=your-aws-secret-key
# MINIO_BUCKET=your-bucket-name
# MINIO_USE_SSL=true

# --------------------------------------------
# CORS設定 (カンマ区切り)
# --------------------------------------------
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 本番環境例
# ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# --------------------------------------------
# フロントエンド設定 (Next.js)
# --------------------------------------------
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=AIプレゼン自動スクリーニング
